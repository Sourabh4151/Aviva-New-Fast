import React from 'react';

// Temporarily disable portal header rendering.
// Returning null keeps imports working for pages that include this component.
const Header = () => {
  return null;
};

export default Header;
